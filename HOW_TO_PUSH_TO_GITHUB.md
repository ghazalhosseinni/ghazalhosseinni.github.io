# 📚 راهنمای کامل: آپلود پروژه به GitHub

این راهنما رو برای **هر پروژه جدیدی** که می‌خواید به GitHub بفرستید استفاده کنید.

---

## 🎯 مرحله 1: ساخت Repository در GitHub

### گام 1-1: ورود به GitHub
1. به سایت GitHub برید: https://github.com
2. با حساب کاربری خود وارد شوید

### گام 1-2: ساخت Repository جدید
1. روی دکمه **"+"** (بالا سمت راست) کلیک کنید
2. **"New repository"** را انتخاب کنید

### گام 1-3: تنظیمات Repository
- **Repository name:** نام پروژه (مثل: `my-vue-project`)
- **Description:** توضیح کوتاه (اختیاری)
- **Public** یا **Private** را انتخاب کنید
- ⚠️ **هیچ چیزی را تیک نزنید!** (نه README، نه .gitignore، نه License)
- روی **"Create repository"** کلیک کنید

---

## 🎯 مرحله 2: آماده‌سازی پروژه محلی

### گام 2-1: باز کردن PowerShell در پوشه پروژه
```powershell
# به پوشه پروژه برید
cd مسیر\پروژه\شما
```

**نکته:** می‌تونید در File Explorer پوشه پروژه رو باز کنید، روی آدرس بار کلیک کنید، `powershell` بنویسید و Enter بزنید.

### گام 2-2: ساخت فایل .gitignore
اول از همه یک فایل `.gitignore` در پوشه اصلی پروژه بسازید:

**برای پروژه Vue/React/Node.js:**
```
node_modules
dist
.env
.DS_Store
```

**برای پروژه Python:**
```
__pycache__
*.pyc
venv
.env
```

---

## 🎯 مرحله 3: آپلود کد به GitHub

### گام 3-1: ایجاد مخزن Git محلی
```powershell
git init
```
این دستور یک مخزن Git در پوشه شما می‌سازد.

### گام 3-2: افزودن فایل‌ها
```powershell
git add .
```
این دستور **همه فایل‌های** پروژه رو برای commit آماده می‌کنه (به جز اونایی که در .gitignore هستند).

**نکته:** اگر فقط یک فایل خاص می‌خواید:
```powershell
git add نام-فایل.js
```

### گام 3-3: اولین Commit
```powershell
git commit -m "Initial commit"
```
این دستور یک snapshot از پروژه می‌گیره با پیام "Initial commit".

**نکته:** پیام commit باید **توضیح‌دهنده** باشه (به انگلیسی).

### گام 3-4: اتصال به GitHub
```powershell
git remote add origin https://github.com/username/repository-name.git
```

**مثال:**
```powershell
git remote add origin https://github.com/ghazalhosseinni/my-vue-project.git
```

⚠️ **مهم:** `username` و `repository-name` رو با اطلاعات خودتون جایگزین کنید!

### گام 3-5: تغییر نام Branch به main
```powershell
git branch -M main
```

### گام 3-6: ارسال به GitHub
```powershell
git push -u origin main
```

اگر اولین بار هست که push می‌کنید، GitHub از شما **username** و **password** می‌خواد:
- **Username:** نام کاربری GitHub
- **Password:** باید یک **Personal Access Token** باشه (نه پسورد واقعی)

---

## 🔑 ساخت Personal Access Token (اگر لازم بود)

### گام 4-1: رفتن به Settings
1. به GitHub برید
2. روی عکس پروفایل (بالا راست) کلیک کنید
3. **Settings** را انتخاب کنید

### گام 4-2: Developer Settings
1. از منوی چپ، به پایین اسکرول کنید
2. **Developer settings** را انتخاب کنید
3. **Personal access tokens** > **Tokens (classic)** را انتخاب کنید

### گام 4-3: ساخت Token جدید
1. **Generate new token** > **Generate new token (classic)**
2. **Note:** بنویسید: "My Projects"
3. **Expiration:** No expiration یا 90 days
4. **Select scopes:** تیک بزنید روی **repo** (همه موارد زیر repo)
5. روی **Generate token** کلیک کنید
6. **توکن را کپی کنید** (فقط یک بار نمایش داده می‌شود!)

### گام 4-4: استفاده از Token
وقتی Git از شما password می‌خواد، **توکن** را paste کنید (نه پسورد واقعی).

---

## 🔄 آپدیت کردن پروژه (تغییرات بعدی)

هر بار که در پروژه تغییری دادید:

### گام 1: بررسی تغییرات
```powershell
git status
```
این دستور لیست فایل‌های تغییر یافته رو نشون میده.

### گام 2: افزودن تغییرات
```powershell
git add .
```
یا برای یک فایل خاص:
```powershell
git add src/App.vue
```

### گام 3: Commit با توضیح
```powershell
git commit -m "Add login feature"
```

**مثال‌های خوب برای پیام commit:**
- `"Add navbar component"`
- `"Fix button styling issue"`
- `"Update README file"`
- `"Remove unused dependencies"`

### گام 4: Push به GitHub
```powershell
git push
```

یا برای اولین بار از یک branch جدید:
```powershell
git push -u origin main
```

---

## 📝 دستورات مفید Git

### بررسی وضعیت
```powershell
git status
```
نشون میده چه فایل‌هایی تغییر کردن.

### دیدن تاریخچه
```powershell
git log
```
لیست همه commit ها رو نشون میده.

### دیدن تاریخچه کوتاه
```powershell
git log --oneline
```

### بررسی تغییرات
```powershell
git diff
```
تغییرات فایل‌ها رو قبل از commit نشون میده.

### لغو تغییرات یک فایل
```powershell
git checkout -- نام-فایل
```

### حذف آخرین commit (محلی)
```powershell
git reset --soft HEAD~1
```

### دیدن Remote URL
```powershell
git remote -v
```

### تغییر Remote URL
```powershell
git remote set-url origin https://github.com/username/new-repo.git
```

---

## 🎯 نکات مهم

### ✅ کارهای درست:
1. همیشه قبل از شروع کار، `git pull` کنید
2. پیام‌های commit واضح و توضیح‌دهنده بنویسید
3. از .gitignore استفاده کنید
4. commit های کوچک و منظم بزنید

### ❌ کارهای اشتباه:
1. node_modules رو به Git اضافه نکنید
2. فایل‌های .env (محتوی API keys) رو push نکنید
3. پسورد واقعی رو به جای token استفاده نکنید
4. بدون توضیح commit نزنید

---

## 🚀 مثال کامل: پروژه جدید

فرض کنید یک پروژه Vue به اسم `my-portfolio` دارید:

```powershell
# 1. رفتن به پوشه پروژه
cd C:\Projects\my-portfolio

# 2. ساخت .gitignore
echo node_modules > .gitignore
echo dist >> .gitignore

# 3. ایجاد مخزن Git
git init

# 4. افزودن فایل‌ها
git add .

# 5. اولین commit
git commit -m "Initial commit: Portfolio project"

# 6. اتصال به GitHub (ابتدا repository در GitHub بسازید)
git remote add origin https://github.com/ghazalhosseinni/my-portfolio.git

# 7. تغییر نام branch
git branch -M main

# 8. Push به GitHub
git push -u origin main
```

---

## 🎉 تبریک!

حالا بلدید پروژه‌هاتون رو به GitHub بفرستید! 

برای **هر پروژه جدید:**
1. Repository در GitHub بسازید
2. دستورات بالا رو در پوشه پروژه اجرا کنید
3. تغییرات رو با `git add` → `git commit` → `git push` بفرستید

موفق باشید! 🚀
